{
  "name": "@litemark/shared-protocol",
  "version": "2.0.0",
  "private": true,
  "description": "Shared JSON Lines IPC protocol between LiteMark Rust core and the render sidecar.",
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
