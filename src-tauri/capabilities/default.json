{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal default capability for the LiteMark main window. Grants only core window/event permissions; the sidecar is driven from Rust, not from the webview, so no shell permission is exposed to the frontend.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
